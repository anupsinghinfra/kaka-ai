# Copy (or CoW-clone) packages from the pnpm store instead of hardlinking.
# The global store contains root-owned entries from a past root-run install;
# hardlinking them makes pnpm's chmod on bin files fail with EPERM.
package-import-method=clone-or-copy
