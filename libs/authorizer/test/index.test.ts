import { AUTHORIZER_LIBRARY_NAME } from '../src/index'

describe('@platform/authorizer placeholder', () => {
  test('exposes the library name', () => {
    expect(AUTHORIZER_LIBRARY_NAME).toBe('@platform/authorizer')
  })
})
