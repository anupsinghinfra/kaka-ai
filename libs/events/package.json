{
  "name": "@platform/events",
  "version": "0.1.0",
  "private": true,
  "description": "Publisher for the platform event bus. Every event is validated against contracts/events/envelope.schema.json before it is sent.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.699.0",
    "@platform/contracts": "workspace:*",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
